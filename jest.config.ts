export default {
  // clear mocks after execution
  clearMocks: true,
  // coverage by v8
  coverageProvider: "v8",
  // test runner
  preset: "ts-jest",
  // match all files that end with .spec.ts
  testMatch: ["**/**/*.spec.ts"],
};
